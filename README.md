`c++ gen_unify.cpp -std=c++17 -o gen_unify`

encode берет скомпилированные ядра одного поколения из директории и пишет в отдельный бинарник позиции отличающихся байтов+общую часть массива

decode принимает название процессора и проделывает обратный процесс

test компилирует solve.cl с помощью rocm-6.0.0, прогоняет encode/decode на каждом файле и проверяет, что соответствующие бинарники не различаются
